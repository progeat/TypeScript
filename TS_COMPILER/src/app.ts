// Описание урока в конфиг файле

function log(a: number) {}

// const node = document.querySelector('.test')! as HTMLInputElement;

// node.value = '30';

const found = [{ a: 1 }, { b: 2 }].find((n) => n.a === 3);

found!.a = 3;
